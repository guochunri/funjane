<div class='row'>
  <div class='col-md-12'>
    <!--=== 页面标题 ===-->
    <h2><%= t('account-order-index') %></h2>

    <!--=== 使用者历史订单 ===-->
    <table class='table'>
      <!-- 历史订单列表标题 -->
      <thead>
        <tr>
          <th>#</th>
          <th><%= t('f-order-created-at') %></th>
        </tr>
      </thead>
      <!-- 历史订单列表內容 -->
      <tbody>
        <% @orders.each do |order| %>
          <tr>
            <td>
              <%= link_to(order.id, order_path(order.token)) %>
            </td>
            <td>
              <%= order.created_at.to_s(:long) %>
            </td>
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>
</div>
